<!--
    - Policies are applied in the order they appear.
    - Position <base/> inside a section to inherit policies from the outer scope.
    - Comments within policies are not preserved.
-->
<!-- Add policies as children to the <inbound>, <outbound>, <backend>, and <on-error> elements -->
<policies>
	<!-- Throttle, authorize, validate, cache, or transform the requests -->
	<inbound>
		<base />
		<set-variable name="apiName" value="@((string)context.Api.Name)" />
		<set-variable name="nvName" value="@($"claim-role-{context.Api.Name}")" />
		<set-variable name="nvValue" value="@((string)context.Variables.GetValueOrDefault("nvName"))" />
		<set-variable name="nvValue1" value="{{ @(nvName) }}" />
		<!--<include-fragment fragment-id="PF-SourceAuth" />-->
	</inbound>
	<!-- Control if and how the requests are forwarded to services  -->
	<backend>
		<base />
	</backend>
	<!-- Customize the responses -->
	<outbound>
		<base />
	</outbound>
	<!-- Handle exceptions and customize error responses  -->
	<on-error>
		<base />
		<include-fragment fragment-id="HandleTargetError" />
	</on-error>
</policies>